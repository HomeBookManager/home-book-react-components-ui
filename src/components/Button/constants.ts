export const className = 'Button';

export enum Color {
  primary = 'primary',
  secondary = 'secondary',
  succes = 'success',
  warning = 'warning',
  error = 'warning',
}

export enum Variant {
  default = 'default',
  contained = 'contained',
  outlined = 'outlined',
}
